# 次のステップ - 作業計画

## 📊 現在の状況

### ✅ 完了している項目
- データベースセットアップ（Supabase接続成功）
- 開発サーバーの起動確認
- 基本的なページ構造の作成（LP、検索、ログイン、ランキング、店舗詳細）
- APIエンドポイントの実装
- 認証機能の実装
- UIコンポーネントの作成
- ヘッドラインの変更完了

### ⏳ 次のステップ（優先順位順）

## 1. 初期データの投入（最優先）

現在、データベースに店舗データがないため、検索ページが空の状態です。

### 方法A: Prisma Studioを使用（推奨）
```bash
npm run db:studio
```
ブラウザで `http://localhost:5555` にアクセスして、手動でデータを追加

### 方法B: SupabaseのSQL Editorで直接実行
```sql
-- 用途カテゴリの作成
INSERT INTO purpose_categories (id, name, description, is_active, created_at, updated_at)
VALUES
  (gen_random_uuid(), '初デート', '初めてのデートにおすすめのお店', true, NOW(), NOW()),
  (gen_random_uuid(), '誕生日', '誕生日のデートにおすすめのお店', true, NOW(), NOW()),
  (gen_random_uuid(), '記念日', '記念日のデートにおすすめのお店', true, NOW(), NOW()),
  (gen_random_uuid(), 'カジュアルデート', '気軽に楽しめるデートにおすすめのお店', true, NOW(), NOW()),
  (gen_random_uuid(), '夜のデート', '夜のデートにおすすめのお店', true, NOW(), NOW())
ON CONFLICT (name) DO NOTHING;

-- サンプル店舗データの追加（後で実際のデータに置き換え）
```

## 2. ブラウザでの動作確認

- [ ] トップページ（`http://localhost:3000`）の表示確認
- [ ] 検索ページ（`/search`）の動作確認
- [ ] ログインページ（`/login`）の動作確認
- [ ] ランキングページ（`/ranking`）の動作確認
- [ ] 店舗詳細ページ（`/restaurants/[id]`）の動作確認

## 3. 認証機能のテスト

- [ ] ユーザー登録機能のテスト
- [ ] ログイン機能のテスト
- [ ] セッション管理の確認

## 4. APIエンドポイントの動作確認

- [ ] `GET /api/restaurants` - 店舗一覧の取得
- [ ] `GET /api/restaurants/[id]` - 個別店舗情報の取得
- [ ] `POST /api/auth/signup` - ユーザー登録

## 5. 機能の実装・改善（必要に応じて）

- [ ] 店舗データの本格的な収集方法の検討
- [ ] レビュー機能の実装（投稿・表示）
- [ ] お気に入り機能の実装
- [ ] デートコース提案機能の実装
- [ ] フィルター機能の改善
- [ ] ソート機能の改善

## 推奨される次のアクション

1. **まず初期データを投入**して、検索ページが正常に動作することを確認
2. **ブラウザで各ページを確認**して、デザインやレイアウトをチェック
3. **認証機能をテスト**して、ユーザー登録・ログインが正常に動作することを確認
4. **APIエンドポイントをテスト**して、データの取得が正常に行われることを確認

## 現在の選択肢

### オプション1: 初期データの投入を進める
Prisma StudioまたはSQL Editorを使用して、テストデータを投入します。

### オプション2: ブラウザでの動作確認を進める
現在の状態で各ページを確認し、問題があれば修正します。

### オプション3: 特定の機能の実装・改善
レビュー機能、お気に入り機能など、特定の機能の実装を進めます。

どの作業から進めますか？

