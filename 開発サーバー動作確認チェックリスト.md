# 開発サーバー動作確認チェックリスト ✅

## ✅ 完了した確認項目

1. **開発サーバーの起動**: `npm run dev` で正常に起動 ✅
2. **トップページのレンダリング**: HTMLが正しく生成されている ✅
3. **データベース接続**: Prisma Clientが正常に動作している ✅

## 確認すべき項目

### 1. ブラウザでの確認

**アクセス**: `http://localhost:3000`

確認ポイント：
- [ ] ページが正常に表示される
- [ ] デザイン（黒背景、レイアウト）が正しく表示される
- [ ] ヘッダーが表示される
- [ ] ヒーローセクションが表示される
- [ ] 悩みセクションが表示される
- [ ] フッターが表示される
- [ ] リンクが正しく機能する

### 2. 各ページの確認

#### トップページ
- [ ] `http://localhost:3000` - 正常に表示される

#### ログインページ
- [ ] `http://localhost:3000/login` - 正常に表示される
- [ ] ログインフォームが表示される
- [ ] エラーメッセージが表示されない

#### 検索ページ
- [ ] `http://localhost:3000/search` - 正常に表示される
- [ ] 検索バーが表示される
- [ ] フィルターサイドバーが表示される（またはスケルトン表示）
- [ ] 店舗カードが表示される（データがない場合は「店舗が見つかりません」など）

#### ランキングページ
- [ ] `http://localhost:3000/ranking` - 正常に表示される
- [ ] ランキングリストが表示される（データがない場合は空のリスト）

### 3. APIエンドポイントの確認

#### 認証API
- [ ] `POST /api/auth/signup` - ユーザー登録が動作する（テスト用のリクエストを送信）
- [ ] `GET /api/auth/[...nextauth]` - NextAuth.jsのエンドポイントが動作する

#### 店舗API
- [ ] `GET /api/restaurants` - 店舗一覧が取得できる（データがない場合は空の配列）
- [ ] `GET /api/restaurants/[id]` - 個別店舗情報が取得できる

### 4. データベース接続の確認

- [ ] コンソールにデータベース接続エラーが表示されない
- [ ] Prisma Clientが正常に動作する
- [ ] データベースクエリが実行できる

### 5. エラーの確認

#### コンソールエラー
- [ ] ブラウザの開発者ツール（F12）でエラーが表示されない
- [ ] サーバーサイドのコンソールにエラーが表示されない

#### ログの確認
- [ ] Prismaのクエリログが表示される（開発モード）
- [ ] エラーログが表示されない

## 次のステップ

### 1. 初期データの投入

データベースにテストデータを投入する：

```bash
cd premier-date
npm run db:seed
```

（seed.tsが実装されている場合）

### 2. 認証機能のテスト

1. ユーザー登録のテスト
   - `/login` でサインアップ
   - メールアドレスとパスワードを入力
   - 登録が成功するか確認

2. ログインのテスト
   - 登録したユーザーでログイン
   - セッションが正しく作成されるか確認

### 3. 店舗検索機能のテスト

1. 検索ページで店舗を検索
2. フィルター機能をテスト
3. ソート機能をテスト
4. 店舗詳細ページを確認

## トラブルシューティング

### ページが表示されない場合

```bash
# サーバーを再起動
npm run dev
```

### データベースエラーが発生する場合

```bash
# Prisma Clientを再生成
npm run db:generate

# データベース接続を確認
npx prisma migrate status
```

### 環境変数が読み込まれない場合

`.env`ファイルを確認：

```bash
cat .env
```

## 現在の状態

- ✅ 開発サーバー: 起動中
- ✅ データベース接続: 正常
- ✅ トップページ: 正常にレンダリング
- ⏳ 各ページの動作確認: 待機中
- ⏳ 認証機能のテスト: 待機中
- ⏳ 初期データの投入: 待機中



